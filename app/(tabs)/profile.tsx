import { View, Text } from 'react-native';
import React from 'react';

export default function Profile() {
    return (
        <View style={{ flex: 1, padding: 16 }}>
            <Text className="text-lg">User Profile</Text>
        </View>
    );
}
